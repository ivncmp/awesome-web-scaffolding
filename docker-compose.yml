services:
  awesome-scaffolding:
    build: .
    container_name: awesome-scaffolding
    restart: unless-stopped
    ports:
      - "5173:3000"
    volumes:
      - ./frontend:/app/frontend
      - ./backend:/app/backend
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - VITE_SUPABASE_URL=http://localhost:8000
      - VITE_SUPABASE_ANON_KEY=demo-key
    networks:
      - awesome-net
    depends_on:
      - edge-functions

  edge-functions:
    image: denoland/deno:alpine-2.1.8
    container_name: awesome-edge-functions
    restart: unless-stopped
    working_dir: /app
    command: deno run --allow-net --allow-env --allow-read hello/index.ts
    ports:
      - "8000:8000"
    volumes:
      - ./backend/supabase/functions:/app
    networks:
      - awesome-net

networks:
  awesome-net:
    driver: bridge
